package org.example.do_an_v1.enums;

/**
 * Read-only customer tier used for UX/reporting.
 * Derived from booking history; never persisted.
 */
public enum CustomerTier {
    SILVER,
    GOLD,
    DIAMOND
}
